name: Modareb Auto Pipeline

on:
  workflow_dispatch:
  schedule:
    # ⏰ تشغيل يومي (9:30 صباحاً بتوقيت السعودية = 6:30 UTC)
    - cron: "30 6 * * 0-4"

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      # 1) تحميل المشروع
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2) إعداد بايثون
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3) تثبيت المكتبات
      - name: Install requirements
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      # 4) جلب البيانات من API
      - name: Fetch market data
        run: |
          if [ -f scripts/fetch_api_data.py ]; then
            python scripts/fetch_api_data.py
          else
            echo "No API script found, skipping..."
          fi
        env:
          API_URL: ${{ secrets.API_URL }}
          API_KEY: ${{ secrets.API_KEY }}

      # 5) توليد البوست
      - name: Generate post image
        run: python scripts/generate_post.py

      # 6) تسجيل الإشارة
      - name: Log signal
        run: python scripts/log_signal.py

      # 7) حفظ سجل الإشارات في GitHub
      - name: Commit signal log
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/signals_log.csv
          git commit -m "Update signals log" || echo "No changes to commit"
          git push

      # 8) النشر في Telegram
      - name: Post to Telegram
        run: |
          if [ -f scripts/post_to_telegram.py ]; then
            python scripts/post_to_telegram.py
          else
            echo "Telegram script not found, skipping..."
          fi
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      # 9) حفظ الصورة كملف قابل للتحميل
      - name: Upload post image
        uses: actions/upload-artifact@v4
        with:
          name: modareb-post
          path: output.png
